@model IEnumerable<Category>

<ul class="nav navbar-nav">
	@foreach (var category in Model)
	{
		<li>
			<a href="@Url.Action("FilmsList", "Films", new { categoryName = category.Name })"> @category.Name </a>
		</li>
	}
</ul>